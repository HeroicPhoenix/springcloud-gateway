server:
  port: 8080

spring:
  application:
    name: springcloud-gateway

  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:nacos:8848}

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway